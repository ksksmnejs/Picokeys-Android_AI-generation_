# 更新日志

发布说明（Releases 页面的正文）由 GitHub Actions **从本文件自动抽取**，
手写在这里即可，不必再去改工作流。

格式约定：每个版本一个二级标题，标题里必须带版本号（可带或不带 `v` 前缀、
可带日期），工作流按它匹配 `buildozer.spec` 里的 `version`。

```markdown
## [vX.Y.Z] - YYYY-MM-DD

### 新增
- 某某功能

### 修复
- 某某问题
```

本文件是**版本号与发布说明的唯一来源**。发布流程：

1. 在这里（和 `CHANGELOG.en.md`）加一个新版本的段落，写明改了什么。
2. Actions → **Build Android APK** → Run workflow，其余保持默认。

工作流会：从本文件取出版本号 → 写进 `buildozer.spec` → 用它生成 release tag →
从本文件抽取该版本段落作为 Releases 页面的正文 → 成功后把版本号提交回仓库。

Run workflow 时的可选开关：

| 选项 | 说明 |
| --- | --- |
| `bump` | `auto`（默认，取本文件最新版本号）/ `patch` / `minor` / `major` / `none` |
| `overwrite` | 默认关闭。同名 tag 已存在时工作流会**失败并提示**，以保住历史版本；勾上则原地替换 |
| `draft` / `prerelease` | 发布为草稿 / 预发布 |

构建失败时 job 提前终止，不会消耗版本号。

---

## [v0.2.1] - 2026-09-26

### 修复

- **选不到固件文件**：改用 Android 系统文件管理器（存储访问框架 SAF）。
  原来 App 内置的目录浏览器受分区存储限制，只能列出少数第三方 App 暴露的目录，
  因此看不到存放在别处的固件文件

### 变更

- **移除从 GitHub / URL 获取固件的功能**，不再申请 `INTERNET` 权限，
  App 现在完全不联网；固件文件改为用系统文件管理器从本地选择

## [v0.2.0] - 2026-09-26

### 新增

- **固件刷写页** —— 支持 UF2（RP2040/RP2350）识别与 ESP32 ROM 串口下载协议；
  固件可来自本地文件或 https 地址，加载后自动识别格式与适用芯片
- 文档补齐 RP2040 / RP2350 / ESP32-S2 / S3 四种芯片的差异
- 新增 `CHANGELOG.md`，发布说明改为自动生成
- 版本号可由工作流自动推进：在 CHANGELOG 里加新段落即可，`buildozer.spec` 无需手改
- **从 GitHub 获取官方固件**：固件页新增入口，直接列出上游开源仓库
  `polhenarejos/pico-fido` Releases 里的固件，按芯片分类并只显示每块板最新的稳定版，
  选中后自动下载（需要 `INTERNET` 权限）
- 不再把开发板称作 PicoKey，改为"开发板"
- 构建使用固定签名密钥（`tools/debug.keystore.b64`），升级 APK 可直接覆盖安装，
  不再需要卸载重装；可用仓库 Secret `ANDROID_KEYSTORE_BASE64` 覆盖
- 新增并发控制：再点 Run workflow 会取消正在跑的旧任务，队列不再堆积
- 文档与界面不再描述进入下载模式的按键手势（不同板子方式不同）

### 修复

- 状态栏遮挡顶部内容（读取系统真实状态栏高度作为顶部间距）
- 长英文按钮文字被截断（按钮高度改为跟随换行后的文字高度）
- 固件刷写页点「扫描」时闪退（成功回调里的异常现在会走错误提示，不再终结主循环）

### 变更

- 固件刷写页按流程重排为「1. 选择板子 / 2. 选择固件 / 3. 写入」
- 移除界面里进下载模式的具体按键手势——不同开发板方式不同，写死会误导，
  改为提示参照板子自身说明
- 发布时同名 tag 默认拒绝覆盖，保留历史版本

## [v0.1.0] - 2026-09-25

首个版本。

- USB OTG 连接，三通道扫描（CCID / rescue / FIDO HID）
- 读取设备信息、PHY 配置读写、安全启动
- 重启与进入刷机模式、WINK 闪灯
- 不需要硬件的协议自检
- 中英双语界面与内置中文字体
